fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
aiosqlite==0.20.0
python-dotenv==1.0.1
pydantic==2.9.0

# numpy 2.x has pre-built cp313 Windows wheels — no compiler needed.
# livekit-plugins-google 0.3.0 pins numpy<2 so it is intentionally excluded:
# the LLM is handled by LangChain (langchain-google-genai), not the LiveKit plugin.
numpy>=2.0.0

# LiveKit — 0.8.12 is the last stable 0.8.x release
livekit==0.17.0
livekit-agents==0.8.12
livekit-plugins-deepgram>=0.6.0,<0.7.0
livekit-plugins-cartesia>=0.4.0,<0.5.0

# LangChain — 0.3.7+ dropped the numpy<2.0 constraint
langchain>=0.3.7,<0.4.0
langchain-core>=0.3.15,<0.4.0
langchain-google-genai>=2.0.4,<3.0.0
google-generativeai>=0.8.0,<1.0.0

# Twilio
twilio==9.3.3

# Utilities
httpx==0.27.2
python-multipart==0.0.12
